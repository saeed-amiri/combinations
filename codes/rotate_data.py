import sys
import pandas as pd
import read_lmp_data as relmp

class Doc:
    """rotate the pd datframe of the data around the center of mass
    of the data.
    It will rotate about x axis and counterclocwise
    input:
        Data in the DataFrame
        Angle of rotation
    output:
        DataFrame
    """

if __name__ == "__main__":
    "rotate the data"